# 프로젝트 작업 규칙

백엔드 작업은 [진행 규칙](docs/backend-workflow.md)에 따라 한 작업씩 진행한다.

- 기존 파일의 미완료 변경을 다른 작업에 섞지 않는다. 필요하면 별도 worktree를 사용한다.
- 먼저 노션 카드의 ID, 완료 기준, 선행 작업을 읽는다. 실제 착수한 카드만 `진행 중`으로 바꾼다.
- `main`에서 `backend/<작업-ID>-<설명>` 브랜치를 만든다. 예: `backend/b-03-read-api`.
- 카드 하나당 PR 하나를 원칙으로 한다. 새 범위가 생기면 별도 카드와 PR로 나눈다.
- PR에는 노션 카드, 변경 이유와 동작, 실행한 검증과 결과, API·DB 변경, 남은 작업을 기록한다. `.github/PULL_REQUEST_TEMPLATE/backend.md`를 사용한다.
- 구현과 관련 검증을 마치면 노션을 `확인 요청`으로 바꾼다. 실패한 검사나 해결되지 않은 리뷰를 남긴 채 병합하지 않는다.
- 세션에서 승인된 범위의 PR은 검증 후 squash merge한다. 병합 전에 검사한 PR HEAD가 바뀌지 않았는지 확인한다.
- 실제 `MERGED` 상태와 커밋 SHA를 확인한 뒤 `docs/templates/backend-merge-comment.md` 형식으로 PR 코멘트를 작성한다. 사용자 요청에 따른 이 기록 외에 다른 사람을 멘션하거나 알림 메시지를 보내지 않는다.
- PR URL, 병합 SHA, 검증 결과를 노션 카드에도 기록한 뒤 `완료`로 바꾼다. PR 생성이나 로컬 테스트 통과만으로 완료 처리하지 않는다.
- 노션 기록이 실패하면 GitHub의 실제 결과와 미동기화 상태를 알린다. 확인하지 못한 업데이트를 완료했다고 보고하지 않는다.
- 연결 정보는 환경변수로 받는다. DB 비밀번호, 토큰, 개인 이메일을 커밋·PR·노션에 적지 않는다. 공개 커밋은 GitHub noreply 이메일을 사용한다.

기술 기준: Spring Boot + Supabase PostgreSQL. Java 21과 Gradle Wrapper로 시작한다. Supabase 프로젝트의 생성·운영 DB 변경은 실제 연결 대상과 적용 범위를 확인한 뒤 진행한다.
