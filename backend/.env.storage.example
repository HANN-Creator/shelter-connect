# Server-only key from the same project as .env.supabase. Never use in RN/web.
# Copy to .env.storage (Git ignored), chmod 600, and paste the existing key literally.
# Do not add quotes or source this file.
SUPABASE_SECRET_KEY=
PHOTO_STORAGE_BUCKET=dog-photos
PHOTO_STORAGE_TIMEOUT_SECONDS=5
