# cozy-dog-v1

`cozy-dog-v1.png`는 2026-09-23 프로젝트의 PixelLab 비교 작업에서 생성한 64×64 투명 도트다. 기존 두부 원본을 생성할 때 실제로 사용했던 그림체 참고 이미지와 같은 파일이다. 외부 에셋 팩이나 보호소 사진이 아니다.

- SHA-256: `c3b27756912d4a9ff8ecec227891c79cffe554fd6b21ea03357f15718419b34d`
- 용도: 기준 캐릭터를 만들 때 도트 크기·외곽선·명암·디테일을 참고한다.
- 외형: 입력 사진이 털색·무늬·귀·주둥이·꼬리를 결정한다. 참고 강아지의 색과 외형을 복사하지 않는다.
- 요청: `style_image`로 전달하며 `style_options.color_palette=false`, 나머지 세 항목은 `true`다.

동작 생성에는 이 이미지를 다시 보내지 않는다. 해당 강아지의 BASE 도트를 참조하고, WALK·RUN·BACK_OFF는 확인한 BASE와 체형 좌표로 렌더링한다. 기존 두부 원본이나 이미 저장된 생성 결과를 교체하는 파일이 아니다.

그림체 변경이 필요하면 이 파일을 조용히 덮어쓰지 않고 새 이름으로 버전을 추가한다. 이미지 생성은 동일 요청에서도 달라질 수 있으므로 실제 결과의 외형·그림체 확인은 별도로 필요하다.
